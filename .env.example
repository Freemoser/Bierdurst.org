# Queue-Seiten nur in nicht-öffentlichen Preview-Builds erzeugen:
PREVIEW_QUEUE=false

# Erst nach AdSense-Freigabe eintragen. Beispielwerte werden absichtlich nicht geladen.
PUBLIC_ADSENSE_PUBLISHER_ID=
PUBLIC_CMP_SCRIPT_URL=
